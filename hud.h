#ifndef _HUDTDS_H_
#define _HUDTDS_H_

#define WIDTH 800
#define HEIGHT 480
#define STRIDE WIDTH * 4


#define min(a, b) ((a) < (b) ? (a) : (b))
#define max(a, b) ((a) > (b) ? (a) : (b))

#endif // _HUDTDS_H_
